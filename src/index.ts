import {
    ToastTypes as types,
    create,
    info,
    warning,
    success,
    error,
    system,
    action
} from "./toast";

export default {
    types,
    notify: create,
    info,
    warning,
    success,
    error,
    system,
    action
}
